$("#new_comment").html("Thank you for your review!");
$('.product-reviews').prepend("<%= j render @comment %>");
$("#stars-avg").attr('data-score', "<%= @product.average_rating %>");
refreshRating();
